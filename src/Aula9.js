let v = ['Poe', 'diablo', 'last epoch', 'undecember', 'Torchlight']
//console.log(v.length)
//for(let i=0;i<v.length ;i++){
for(let e of v){
    console.log(e)
}

// comando 'in' pega a casa que esta o vetor.
// comando 'of' pega o valor.